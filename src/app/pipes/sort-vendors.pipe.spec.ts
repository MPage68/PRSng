import { SortVendorsPipe } from './sort-vendors.pipe';

describe('SortVendorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortVendorsPipe();
    expect(pipe).toBeTruthy();
  });
});
