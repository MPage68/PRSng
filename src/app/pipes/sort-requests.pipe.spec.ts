import { SortRequestsPipe } from './sort-requests.pipe';

describe('SortRequestsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRequestsPipe();
    expect(pipe).toBeTruthy();
  });
});
